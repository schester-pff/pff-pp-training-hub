[build]
  functions = "netlify/functions"
 
[functions]
  node_bundler = "esbuild"
 
[functions."coach"]
  timeout = 30
 
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
 
